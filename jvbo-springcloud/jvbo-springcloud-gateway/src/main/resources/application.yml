server:
  port: 8000
spring:
  application:
    name: jvbo-springcloud-gateway
    
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8700/eureka/

logging:
  level:
    root: info
    com.jvbo: debug